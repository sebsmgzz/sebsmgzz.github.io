import "./Header.scss";

export type HeaderProps = {

}

export const Header = function(props: HeaderProps) {
    return (
        <header>
            Header
        </header>
    );
}

export default Header;
